<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/public/css/itemwrite.css" />
  <link rel="stylesheet" href="https://uicdn.toast.com/editor/latest/toastui-editor.min.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Document</title>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <script src="https://uicdn.toast.com/editor/latest/toastui-editor-all.min.js"></script>
</head>

<body>
  <div class="mainbox">
    <form name="updateData">
      <div class="selectimgbox">
        <!-- 이미지 -->
        <label for="image" id="select-img">
          <div class="preimgbox"><img class="preimg" src="<%=data[0].url %>" alt=" 선택한 이미지" /></div>
        </label>
        <input id="image" oninput="binImg()" type="file" name="image" />
      </div>
      <div class="input-group">
        <label for="types">타입</label>
        <select name="type" id="types">
          <% webtoondata.forEach((x)=> { %>
            <option value="<%= x.id %>" <%=x.id===data[0].cateid ? "selected" : "" %>>
              <%= x.name %>
            </option>
            <% }) %>
        </select>
      </div>

      <div class="input-group">
        <label for="name">상품명</label>
        <input id="name" name="name" value="<%= data[0].name %>" />
      </div>

      <div class="input-group">
        <label for="price">가격</label>
        <input id="price" type="number" name="price" value="<%= data[0].price %>" />
      </div>

      <div id="content" class="content-box">
        <%= data[0].detail %>
      </div>
      <div class="btnbox">
        <button class="btn" onclick="updateForm(event, `<%= data[0].id %>`)">수정</button>
      </div>

    </form>
  </div>
</body>
<script src="/public/js/itemwrite.js"></script>

</html>